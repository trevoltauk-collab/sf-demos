spring:
  application:
    name: doc-gen-service
  config:
    import: optional:configserver:
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 6
